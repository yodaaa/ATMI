<%= form_for @chat, :html => { :class => "form-horizontal chat" } do |f| %>

  <% if @chat.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@chat.errors.count, "error") %> prohibited this chat from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @chat.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:name) %>
  </div>
  <div class="form-group">
    <%= f.label :mesg, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :mesg, :class => 'form-control' %>
    </div>
    <%=f.error_span(:mesg) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                chats_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
